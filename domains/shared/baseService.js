'use strict';

/**
 * Parent function implementing shared functionality across service tiers
 */
class BaseService {
  /**
   * Initialize and instance of the BaseService
   * @constructor
   * @param {object} opts - IoC Container
   */
  constructor(opts) {
  }
}

module.exports = BaseService;
